{
    "code": "// Fluid energy over the bubble shape\nvec3 p = pModel * 5.0;\nfloat distorsione = sin(p.x) * sin(p.y + ubuf.u_time) * sin(p.z);\nfloat energia = sin(length(p) * 3.0 + distorsione * 4.0 - ubuf.u_time * 3.0);\nenergia = energia * 0.5 + 0.5;\n\n// u_col2 = dark background, u_col1 = glowing energy (dock colors)\ntextureCol = mix(ubuf.u_col2, ubuf.u_col1, energia);",
    "color1": "#5cc8ff",
    "color2": "#000000",
    "displacement": "// Compute a 3D relief\nfloat rilievo = sin(pModel.x * 20.0) * sin(pModel.y * 20.0) * sin(pModel.z * 20.0);\n\n// Subtract it from the surface (expanding it and creating the bumps)\nd_surf -= rilievo * 0.05;",
    "hasCustomColors": true,
    "isImplicitMode": true,
    "name": "Animated Iridescence1",
    "pan_x": 0,
    "pan_y": 0,
    "rotation": 0,
    "type": "custom_texture",
    "zoom": 1
}