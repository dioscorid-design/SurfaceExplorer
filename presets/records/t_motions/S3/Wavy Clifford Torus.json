{
    "angles": {
        "omega": 1.6699987649917603,
        "phi": 1.3699990510940552,
        "psi": 1.0499993562698364
    },
    "background": {
        "code": "// =========================================================================\n// Title: Riemann peaks\n// Author: iruka\n// Source: https://www.shadertoy.com/view/3XKBWz\n//\n// License: CC BY-NC-SA 3.0 (Shadertoy Default)\n//          https://creativecommons.org/licenses/by-nc-sa/3.0/\n//\n// Description:\n// A raymarching shader that visualizes a procedural mathematical landscape \n// generated by an optimized Riemann function. It features a continuous \n// flyover camera, height-based spectral color palettes, and atmospheric \n// depth fog with diffuse and Fresnel lighting effects.\n// =========================================================================\n\n#define CAMERA_HEIGHT 3.4\n#define CAMERA_TILT - 1.2\n#define FLYOVER_SPEED 1.3\n#define FOVY 1.8\n\n#define FOG_COLOR vec3(0.00, 0.001, 0.015)\n#define SKY_COLOR vec3(0.00, 0.00, 0.01)\n#define FOG_DENSITY 0.001\n\n#define N_SUM 14\n#define MAX_STEPS 100\n#define MAX_DIST 200.0      \n\n// Spectral Color Palette\nvec3 getPalette(float t) {\n    return 0.5 + 0.5 * cos(6.28318 * (t + vec3(0.26, 0.41, 0.55)));\n}\n\n// Optimized Height Function\nfloat riemann(float x, float z) {\n    vec2 res = vec2(0.0);\n    for (int n = 1; n <= N_SUM; n++) {\n        float n2 = float(n * n);\n        float phase = n2 * x;\n        float shift = float(n) * z;\n        res += (1.0/n2) * 2.0 * cos(shift) * vec2(cos(phase) - 1.0, sin(phase));\n    }\n    return length(res) * 0.5;\n}\n\nfloat map(vec3 p) {\n    return (p.y - riemann(p.x, p.z)) * 0.4; \n}\n\nvec3 getNormal(vec3 p) {\n    vec2 e = vec2(0.01, 0.0);\n    return normalize(vec3(map(p + e.xyy) - map(p - e.xyy),\n                          map(p + e.yxy) - map(p - e.yxy),\n                          map(p + e.yyx) - map(p - e.yyx)));\n}\n\nvoid mainImage(out vec4 fragColor, in vec2 fragCoord) {\n    vec2 uv = (fragCoord - 0.5 * iResolution.xy) / iResolution.y;\n\n    // Setup Camera\n    vec3 ro = vec3(iTime * FLYOVER_SPEED, CAMERA_HEIGHT, iTime * FLYOVER_SPEED); \n    vec3 fd = normalize(vec3(1.0, CAMERA_TILT, 1.0)); \n    vec3 rt = normalize(cross(vec3(0, 1, 0), fd));\n    vec3 rd = normalize(fd + (uv.x * rt + uv.y * cross(fd, rt)) * FOVY);\n\n    // Background Gradient\n    vec3 bg = mix(FOG_COLOR, SKY_COLOR, rd.y * 0.5 + 0.5);\n\n    // Raymarching\n    float d = 0.0;\n    for(int i = 0; i < MAX_STEPS; i++) {\n        float s = map(ro + rd * d);\n        d += s;\n        if(d > MAX_DIST || s < 0.0005) break;\n    }\n\n    vec3 col = bg;\n\n    if(d < MAX_DIST) {\n        vec3 p = ro + rd * d;\n        vec3 n = getNormal(p);\n        \n        // Lighting: Diffuse + Fresnel\n        float dif = max(dot(n, normalize(vec3(0.5, 0.8, 0.2))), 0.0);\n        float fre = pow(clamp(1.0 + dot(rd, n), 0.0, 1.0), 4.0);\n        \n        // Final Surface Color\n        col = getPalette(p.y * 0.5 + n.y * 0.1) * (dif + 0.3);\n        col += fre * vec3(0.4, 0.7, 1.0); // Edge glow\n        col *= smoothstep(-0.2, 1.0, p.y); // Vertical shading\n        \n        // Fog Apply\n        col = mix(bg, col, exp2(-FOG_DENSITY * d * d));\n    }\n\n    fragColor = vec4(pow(col, vec3(0.4545)), 1.0);\n}",
        "col1": "#33cc33",
        "col2": "#000000",
        "color": "#664d4d",
        "enabled": true,
        "pan_x": 0,
        "pan_y": 0,
        "rotation": 0,
        "zoom": 1
    },
    "camera3D": {
        "pitch": 0,
        "roll": 0,
        "rot_w": 0.004869387019425631,
        "rot_x": 0.7511728405952454,
        "rot_y": 0.46526604890823364,
        "rot_z": 0.4682340621948242,
        "x": 0,
        "y": 0,
        "yaw": 0,
        "z": 4
    },
    "colors": {
        "alpha": 1,
        "bordColor": "#33cc33",
        "surfColor": "#33cc33"
    },
    "constants": {
        "A": 0.9800000190734863,
        "B": 1,
        "C": 1,
        "D": 1,
        "E": 1,
        "F": 1,
        "S": 0
    },
    "equations": {
        "defU": "",
        "defV": "",
        "defW": "",
        "explicitU": "",
        "explicitV": "",
        "explicitW": "",
        "p": "A * sqrt(0.5 - 0.4 * cos(3.0*u + 2.0*v - 2.0*t)) * sin(v)",
        "x": "A * sqrt(0.5 + 0.4 * cos(3.0*u + 2.0*v - 2.0*t)) * cos(u)",
        "y": "A * sqrt(0.5 + 0.4 * cos(3.0*u + 2.0*v - 2.0*t)) * sin(u)",
        "z": "A * sqrt(0.5 - 0.4 * cos(3.0*u + 2.0*v - 2.0*t)) * cos(v)"
    },
    "lightIntensity": 1,
    "lightingMode": 0,
    "limits": {
        "uMax": 6.2831854820251465,
        "uMin": 0,
        "vMax": 6.2831854820251465,
        "vMin": 0,
        "wMax": 0,
        "wMin": 0
    },
    "name": "Wavy Clifford Torus",
    "observer4D": 4,
    "path3D": {
        "roll": "",
        "x": "",
        "y": "",
        "z": ""
    },
    "path4D": {
        "alpha": "",
        "beta": "",
        "gamma": "",
        "w": "",
        "x": "",
        "y": "",
        "z": ""
    },
    "pathMode": 0,
    "projectionMode": 1,
    "renderMode": 1,
    "showBorder": false,
    "speeds": {
        "nutation": 0,
        "omega": 0,
        "path3D": 1,
        "path4D": 1,
        "phi": 0,
        "precession": 0,
        "psi": 0,
        "spin": 0
    },
    "steps": 203,
    "texture": {
        "code": "//SOUND_BEGIN\n//SOUND_BEGIN\n// =========================================================================\n// Title: Dust Storm Sonification (Pure Wind/Sand) =========================================================================\n\n// --- Le funzioni matematiche originali ---\nfloat tri(in float x) { \n    return abs(fract(x) - 0.5); \n}\n\nvec3 tri3(in vec3 p) { \n    return vec3(tri(p.z + tri(p.y * 1.0)), tri(p.z + tri(p.x * 1.0)), tri(p.y + tri(p.x * 1.0))); \n}\n\n// Generatore di turbolenza frattale 3D (il vento)\nfloat triNoise3d(in vec3 p, in float spd, in float time) {\n    float z = 1.4;\n    float rz = 0.0;\n    vec3 bp = p;\n    for (float i = 0.0; i <= 3.0; i++) {\n        vec3 dg = tri3(bp * 2.0);\n        p += (dg + time * spd);\n        bp *= 1.8;\n        z *= 1.5;\n        p *= 1.2;\n        rz += (tri(p.z + tri(p.x + tri(p.y)))) / z;\n        bp += 0.14;\n    }\n    return rz;\n}\n\n// --- Generazione Audio ---\nvec2 mainSound(int samp, float time) {\n    // 1. Muoviamo i \"microfoni virtuali\" attraverso il rumore 3D\n    vec3 pL = vec3(time * 150.0, time * 50.0, 0.0);\n    vec3 pR = vec3(time * 140.0, time * 60.0, 10.0);\n    \n    // 2. Estraiamo il rumore della tempesta\n    float noiseL = triNoise3d(pL, 0.0, time);\n    float noiseR = triNoise3d(pR, 0.0, time);\n    \n    // 3. Centriamo l'onda (il triNoise restituisce solo valori positivi) \n    // e ne regoliamo l'ampiezza per dare dinamica alla sabbia\n    noiseL = (noiseL - 0.4) * 2.5;\n    noiseR = (noiseR - 0.4) * 2.5;\n    \n    // 4. Mix finale: Solo il fruscio del vento e della sabbia, leggermente abbassato (80%)\n    vec2 audio = vec2(noiseL, noiseR) * 0.8;\n    \n    // 5. Inviluppo per entrare dolcemente\n    float env = smoothstep(0.0, 2.0, time);\n    \n    return clamp(audio * env, -1.0, 1.0);\n}\n//SOUND_END\n\nvoid mainImage( out vec4 fragColor, in vec2 fragCoord )\n{\n    // Normalized pixel coordinates (from 0 to 1)\n    vec2 uv = fragCoord/iResolution.xy;\n\n    // Time varying pixel color\n    vec3 col = 0.5 + 0.5*cos(iTime+uv.xyx+vec3(0,2,4));\n\n    // Output to screen\n    fragColor = vec4(col,1.0);\n}",
        "col1": "#33cc33",
        "col2": "#000000",
        "enabled": true,
        "pan_x": 0,
        "pan_y": 0,
        "rotation": 0,
        "zoom": 1
    },
    "type": "motion",
    "use4DLighting": true
}
