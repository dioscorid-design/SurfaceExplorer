{
    "angles": {
        "omega": 0,
        "phi": 0,
        "psi": 0
    },
    "background": {
        "code": "//MUSIC: /home/dioscorid/Projects/presets/Sounds/Mp3/845832__frame__complex-shifting-ambient-drone-9.mp3\n\n// --- PARAMETRI ---\nfloat zoom = 1.0;       // Collegato alla densità/zoom dell'UI\nvec2 center = vec2(-0.5, 0.0); // Centro del frattale\nint max_iter = 100;     // Qualità del dettaglio\n// --- MAPPATURA COORDINATE ---\n// Trasformiamo u,v (0.0 - 1.0) nel piano complesso\n// Moltiplichiamo per 3.0 per avere un'inquadratura iniziale classica.\n// Dividiamo per 'zoom' per ingrandire.\nvec2 c = (vec2(u, v) - 0.5) * (3.0 / zoom) + center;\n// --- LOGICA MANDELBROT (Z = Z^2 + C) ---\nvec2 z = vec2(0.0);\nint iter = 0;\nfor (int i = 0; i < 100; i++) { // Loop fisso per GLSL (usa max_iter per il break)\n    if (i >= max_iter) break;\n    // Ottimizzazione: se la distanza al quadrato > 4, è scappato\n    if (dot(z, z) > 4.0) break;\n    // Calcolo numero complesso: (x + iy)^2 + c\n    // Parte Reale: x^2 - y^2 + cx\n    // Parte Immaginaria: 2xy + cy\n    float x = (z.x * z.x - z.y * z.y) + c.x;\n    float y = (2.0 * z.x * z.y) + c.y;\n    z = vec2(x, y);\n    iter++;\n}\n// --- COLORAZIONE ---\n// Normalizziamo le iterazioni tra 0.0 e 1.0\nfloat t = float(iter) / float(max_iter);\n// Usiamo una radice quadrata (pow 0.5) per rendere il gradiente più visibile\n// e meno \"tutto nero\" ai bordi.\nt = sqrt(t);\n// Mix tra i colori dell'utente\n// t=0 (esterno immediato) -> Colore 1\n// t=1 (interno insieme)   -> Colore 2\nreturn mix(u_col1, u_col2, t);",
        "col1": "#1c3664",
        "col2": "#e28d57",
        "color": "#4d4d4d",
        "enabled": true,
        "pan_x": 0,
        "pan_y": 0,
        "rotation": 0,
        "zoom": 1
    },
    "camera3D": {
        "rot_w": 1,
        "rot_x": 0,
        "rot_y": 0,
        "rot_z": 0,
        "x": 0,
        "y": 0,
        "z": 4
    },
    "colors": {
        "bordColor": "#33cc33",
        "surfColor": "#33cc33"
    },
    "constants": {
        "A": 0.4000000059604645,
        "B": 3,
        "C": 1,
        "D": 1,
        "E": 1,
        "F": 1,
        "S": 0.5
    },
    "equations": {
        "explicitU": "",
        "explicitV": "",
        "explicitW": "",
        "p": "",
        "x": "",
        "y": "",
        "z": ""
    },
    "lightingMode": 0,
    "limits": {
        "uMax": 6.283180236816406,
        "uMin": 0,
        "vMax": 6.283180236816406,
        "vMin": 0,
        "wMax": 0.10000000149011612,
        "wMin": 0
    },
    "name": "Otto Tube",
    "observer4D": 12.100030899047852,
    "path3D": {
        "roll": "",
        "x": "",
        "y": "",
        "z": ""
    },
    "path4D": {
        "alpha": "",
        "beta": "",
        "gamma": "",
        "w": "",
        "x": "",
        "y": "",
        "z": ""
    },
    "projectionMode": 1,
    "scriptCode": "// --- CONFIGURAZIONE ---\nu_min := 0;\nu_max := 6.28318; \nv_min := 0;\nv_max := 6.28318;\nsteps := 350;\n\ns := 0.5; // Velocità del respiro 4D\nA := 0.4; // Spessore del tubo\nB := 3; // Moltiplicatore Altezza 4D (portalo > 1.0 per la singolarità)\n\n// --- CODICE SHADER ---\n\nfloat r = A; \n\n// Animazione: Respiro nel tempo 't' controllato da 's'.\nfloat lift4D = (sin(t * s) + 1.0) * (B * 2.0); \n\n// --- 1. CURVA CENTRALE (Nodo a Otto) ---\nvec4 Curve4D = vec4(\n    (2.5 + cos(2.0 * u)) * cos(3.0 * u), // X\n    (2.5 + cos(2.0 * u)) * sin(3.0 * u), // Y\n    1.2 * sin(4.0 * u),                  // Z (crea l'incrocio in 3D)\n    lift4D * sin(2.0 * u)                // W (L'escursione 4D)\n);\n\n// Estraiamo la parte 3D\nvec3 Curve = Curve4D.xyz;\n\n// --- 2. DERIVATA (Direzione della curva) ---\nfloat dr = -2.0 * sin(2.0 * u);\nvec3 dC = vec3(\n    dr * cos(3.0 * u) - (2.5 + cos(2.0 * u)) * 3.0 * sin(3.0 * u),\n    dr * sin(3.0 * u) + (2.5 + cos(2.0 * u)) * 3.0 * cos(3.0 * u),\n    4.8 * cos(4.0 * u)\n);\nvec3 Tangent = normalize(dC);\n\n// --- 3. COSTRUZIONE DEL FRAME ---\nvec3 Up = vec3(0.0, 0.0, 1.0);\nvec3 Binorm = cross(Tangent, Up);\nif (length(Binorm) < 0.001) Binorm = cross(Tangent, vec3(0.0, 1.0, 0.0));\nBinorm = normalize(Binorm);\nvec3 Norm = cross(Binorm, Tangent);\nNorm = normalize(Norm);\n\n// --- 4. COSTRUZIONE DEL TUBO ---\nfloat tube_cos = r * cos(v);\nfloat tube_sin = r * sin(v);\n\nvec3 pos3D = Curve + (tube_cos * Norm + tube_sin * Binorm);\n\n// Output finale\nreturn vec4(pos3D * 0.35, Curve4D.w);",
    "showBorder": false,
    "speeds": {
        "nutation": 0,
        "omega": 0,
        "phi": 0,
        "precession": 0,
        "psi": 0,
        "spin": 0
    },
    "steps": 350,
    "texture": {
        "code": "//MUSIC: /home/dioscorid/Projects/presets/Sounds/Mp3/845832__frame__complex-shifting-ambient-drone-9.mp3\n\n// --- PARAMETRI ---\n// Imposta nel dock: Rosso (c1) e Bianco (c2) per l'effetto classico\nvec3 c1 = u_col1;\nvec3 c2 = u_col2;\n// --- CONFIGURAZIONE SPIRALE ---\n// Coils: Quante strisce vedi in una sezione trasversale del tubo\nfloat coils = 6.0;\n// Twists: Quanti giri completi fa la spirale lungo tutto il toro\n// Deve essere un numero intero per evitare che la texture si tagli alla giunzione\nfloat twists = 40.0;\n// --- CALCOLO ---\n// La magia è sommare U e V moltiplicati.\n// (u * twists) spinge la texture lungo il percorso\n// (v * coils) avvolge la texture attorno al tubo\n// Sommando i due si ottiene una linea diagonale (elica/spirale)\nfloat pos = (u * twists) + (v * coils);\n// --- CREAZIONE STRISCE NETTE ---\n// fract(pos) crea un dente di sega da 0.0 a 1.0 ripetuto\n// step(0.5, ...) taglia di netto il valore: sotto 0.5 è 0, sopra è 1.\n// Calcola quanto è \"spesso\" un pixel a questa distanza\nfloat fw = fwidth(pos); \n// Usa smoothstep per sfumare dolcemente il bordo esattamente della larghezza di un pixel\nfloat stripe = smoothstep(0.5 - fw, 0.5 + fw, fract(pos));\nreturn mix(c1, c2, stripe);",
        "col1": "#33cc33",
        "col2": "#000000",
        "enabled": true,
        "pan_x": 0,
        "pan_y": 0,
        "rotation": 0,
        "zoom": 1
    },
    "type": "motion",
    "use4DLighting": true
}
